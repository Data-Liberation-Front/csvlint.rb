rvm:
- 2.0.0
before_install:
  - sudo apt-get update
  - sudo apt-get install libicu-dev
notifications:
  irc:
    channels:
      - "irc.freenode.net#theodi"
    template:
      - "%{repository} %{branch} - %{message} %{build_url}"
    on_success: change
    on_failure: always